let submitForm = document.querySelector(".submitForm").addEventListener("click", (e) => {
    e.preventDefault()
})

let submitBtn = document.querySelector(".submitBtn").addEventListener("click", () => {
    alert("The BackEnd is still in process")
})